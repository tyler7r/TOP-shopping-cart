const Cart = (props) => {
    console.log(props.cart);
    // return (
    //     <div>
    //         {props.cart.map(item => {
    //             return (
    //                 <div key={item.name}className='cartItem'>{item}</div>
    //             )
    //         })}
    //     </div>
    // )
}

export default Cart;